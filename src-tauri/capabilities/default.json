{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Main capability for the ResendBox window",
  "windows": ["main"],
  "permissions": ["core:default", "opener:default"]
}
